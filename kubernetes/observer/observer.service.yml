apiVersion: v1
kind: Service
metadata:
  name: observer
  labels:
    version: ${VERSION}
spec:
  selector:
    app: observer
    version: ${VERSION}
  ports:
  - port: 9876
    targetPort: 9876
    name: p2p
    protocol: TCP
