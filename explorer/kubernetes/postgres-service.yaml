apiVersion: v1
kind: Service
metadata:
  labels:
    app: explorer-postgres
  name: explorer-postgres
spec:
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
  selector:
    app: explorer-postgres
